--- a/src/spektrafilm/model/grain.py
+++ b/src/spektrafilm/model/grain.py
@@ -1,3 +1,5 @@
+import gc
+
 import numpy as np
 import scipy
 import scipy.ndimage
@@ -196,6 +198,9 @@
         density_curves_layers,
         positive_film=profile_type == 'positive',
     )
+    # Cold interpolation can retain a repeated RGB argument in compiler cycles.
+    # Keep the returned layers alive while reclaiming dead owners before grain.
+    gc.collect()
     density_max_layers = np.nanmax(density_curves_layers, axis=0)
     return apply_grain_to_density_layers(
         density_cmy_layers,
