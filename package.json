{
  "name": "slipstream",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.0",
  "engines": {
    "bun": "1.4.0"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --workspaces --if-present build",
    "format": "prettier --write .github apps CONTRIBUTING.md README.md eslint.config.js package.json tsconfig.json vitest.config.ts",
    "format:check": "prettier --check .github apps CONTRIBUTING.md README.md eslint.config.js package.json tsconfig.json vitest.config.ts",
    "lint": "eslint .",
    "test": "vitest run",
    "test:fast": "bun run lint && bun run typecheck && bun run test",
    "typecheck": "bun run --workspaces --if-present typecheck",
    "verify": "bun run format:check && bun run test:fast && bun run build"
  },
  "devDependencies": {
    "@eslint/js": "9.34.0",
    "@types/bun": "1.4.0",
    "eslint": "9.34.0",
    "eslint-config-prettier": "10.1.8",
    "globals": "16.3.0",
    "prettier": "3.6.2",
    "typescript": "5.9.2",
    "typescript-eslint": "8.41.0",
    "vite": "7.1.3",
    "vitest": "3.2.4"
  }
}
